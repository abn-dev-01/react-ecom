// Extends Vitest's `expect` with jest-dom matchers (toBeInTheDocument,
// toBeDisabled, toHaveTextContent, etc.) — imported once here so every
// test file gets them without a per-file import.
import "@testing-library/jest-dom/vitest";
